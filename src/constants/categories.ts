const categories = [
  'babel',
  'eslint',
  'testing-library',
  '@types',
  '@ma-js-common'
];

const lintFilter = ['prettier', 'eslint', 'stylelint'];
const buildFilter = ['typescript', 'react-scripts', 'next', 'rollup', 'sass'];
export { categories, lintFilter, buildFilter };
